# Homebrew Tap for casi

Install the latest release with:

```sh
brew install francodipietro/tap/casi
```

`casi` syncs coding-assistant sessions between machines over Git remotes. Its
source and release attestations are at
[`francodipietro/casi`](https://github.com/francodipietro/casi).
